import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';

// شاشة "تيباروز (التجمع)"

// شاشة "تيباروز (العجمي)"

// شاشة "تيباروز (الساحل الشمالى)"
class TibaRoseNorthCoastScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("تيباروز (الساحل الشمالى)", style: GoogleFonts.cairo(fontSize: 22, fontWeight: FontWeight.bold)),
        centerTitle: true,
      ),
      body: Center(
        child: Text(
          "مرحبًا بك في تيباروز (الساحل الشمالى)",
          style: GoogleFonts.cairo(fontSize: 24, fontWeight: FontWeight.bold),
          textAlign: TextAlign.center,
        ),
      ),
    );
  }
}

// شاشة "تيباروز (اللاجون مطروح)"
class TibaRoseMatrouhLagoonScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("تيباروز (اللاجون مطروح)", style: GoogleFonts.cairo(fontSize: 22, fontWeight: FontWeight.bold)),
        centerTitle: true,
      ),
      body: Center(
        child: Text(
          "مرحبًا بك في تيباروز (اللاجون مطروح)",
          style: GoogleFonts.cairo(fontSize: 24, fontWeight: FontWeight.bold),
          textAlign: TextAlign.center,
        ),
      ),
    );
  }
}

// شاشة "تيباروز (الريفيرا مطروح)"
class TibaRoseMatrouhRivieraScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("تيباروز (الريفيرا مطروح)", style: GoogleFonts.cairo(fontSize: 22, fontWeight: FontWeight.bold)),
        centerTitle: true,
      ),
      body: Center(
        child: Text(
          "مرحبًا بك في تيباروز (الريفيرا مطروح)",
          style: GoogleFonts.cairo(fontSize: 24, fontWeight: FontWeight.bold),
          textAlign: TextAlign.center,
        ),
      ),
    );
  }
}

// شاشة "تيباروز (الغردقة)"
class TibaRoseHurghadaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("تيباروز (الغردقة)", style: GoogleFonts.cairo(fontSize: 22, fontWeight: FontWeight.bold)),
        centerTitle: true,
      ),
      body: Center(
        child: Text(
          "مرحبًا بك في تيباروز (الغردقة)",
          style: GoogleFonts.cairo(fontSize: 24, fontWeight: FontWeight.bold),
          textAlign: TextAlign.center,
        ),
      ),
    );
  }
}

// شاشة "تيباروز (الليدو - طلعت حرب)"
class TibaRoseLidoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("تيباروز (الليدو - طلعت حرب)", style: GoogleFonts.cairo(fontSize: 22, fontWeight: FontWeight.bold)),
        centerTitle: true,
      ),
      body: Center(
        child: Text(
          "مرحبًا بك في تيباروز (الليدو - طلعت حرب)",
          style: GoogleFonts.cairo(fontSize: 24, fontWeight: FontWeight.bold),
          textAlign: TextAlign.center,
        ),
      ),
    );
  }
}
